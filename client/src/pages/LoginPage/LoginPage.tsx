import {
    Base
} from "../../layouts";

function LoginPage() {
    return (
        <Base documentTitle={ "Login | Fitnefier" }>
            <h1>Login page</h1>
        </Base>
    )
}

export default LoginPage;